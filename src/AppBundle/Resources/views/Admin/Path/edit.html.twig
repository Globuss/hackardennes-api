{% extends '::base.html.twig' %}

{% block body %}
    <form action="{{ path('admin_paths_edit', {id: path.id}) }}" method="POST" class="form form-horizontal">

        {{ form_widget(form) }}

        <button type="submit">Enregistrer</button>
    </form>
{% endblock %}
